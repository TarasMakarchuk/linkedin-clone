export enum FriendRequestStatusEnum {
    NOT_SENT = 'not-sent',
    PENDING = 'pending',
    ACCEPTED = 'accepted',
    DECLINED = 'declined',
    WAITING_TO_CURRENT_USER_RESPONSE = 'waiting-to-current-user-response',
}
